from math import sin, exp


def sine_function(x: float) -> float:
    """
    Calculate the sine of a number x.
    """
    return sin(x)


def exponential_function(x: float) -> float:
    """
    Calculate the exponential of a number x.
    """
    return exp(x)
